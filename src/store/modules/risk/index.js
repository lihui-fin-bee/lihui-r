import team from './team'
import indicator from './indicator'

const state = {
}

const mutations = {
}

const actions = {
}

const modules = {
  team,
  indicator
}

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  modules
}
